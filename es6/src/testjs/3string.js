
//  includes 包含   startWith 前面搜索   endsWith 后面搜索   (对象，开始位置)
// repeat 重复原来的字符串次数
  
  'x'.repeat(3);   // xxx   参数取整
  
  // padStart  头补齐   padEnd 尾补齐   用来补齐前缀
  'x'.padStart(5,'ab')  //ababx
  'x'.padEnd(5,'ab')  //xabab
  
  var a = 99;
  
  let c = `dfa${a}fa`;
  let d = "  g56  ";    //  d.trim()   去空格
 
